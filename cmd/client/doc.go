/*
Package main реализует точку входа клиентского приложения GophKeeper.

Основные задачи пакета:
- Загрузка конфигурации приложения из файла или переменных окружения.
- Инициализация логгера с ротацией файлов и уровнем логирования.
- Настройка и создание менеджера gRPC соединений с сервером.
- Создание и настройка менеджера авторизации (AuthManager) с хранением токена.
- Организация обработки системных сигналов для корректного завершения.
- Запуск TUI-интерфейса приложения с переданными сервисами.

Основные функции:
- main: вызывает run и обрабатывает критические ошибки.
- run: реализует пошаговую инициализацию, подготовку сервисов и запуск TUI.

Структура взаимодействия:
- config — загрузка конфигурации;
- logger — инициализация логирования;
- connection.Manager — управление gRPC соединениями;
- auth.AuthManager — авторизация и управление токеном;
- tui.AppServices — агрегатор сервисов для TUI;
- tui.Run — запуск интерактивного пользовательского интерфейса.

Пример:

	cfg, err := config.Load()
	if err != nil {
		// обработка ошибки
	}
	...
	err = tui.Run(ctx, services)
*/
package main
